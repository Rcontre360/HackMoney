import { CreateLendingComponent } from "@shared/components/app/createLending/createLendingForm";
import { PoolFormComponent } from "@shared/components/poolForm/poolForm";
import Layout from "@shared/layout";
// import React from "react";

const CreateLendingForm = () => {
  return (
    <Layout>
      <CreateLendingComponent />
    </Layout>
  );
};

export default CreateLendingForm;
